{
  "name": "nikke-calc-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm run sync-runtime && npm run build-guides",
    "dev": "vite",
    "pretest": "npm run sync-runtime && npm run build-guides",
    "test": "vitest",
    "sync-runtime": "node scripts/sync-runtime.mjs",
    "build-pickup-history": "node scripts/build-pickup-history.mjs",
    "check-runtime": "node scripts/check-runtime.mjs",
    "check-pages": "node scripts/check-pages.mjs",
    "prebuild": "npm run sync-runtime && npm run check-runtime && npm run build-guides",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "build-guides": "node scripts/build-guides.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "jsdom": "^29.1.1",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10",
    "yaml": "^2.9.0"
  }
}
