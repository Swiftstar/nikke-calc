# 블라블라링크 조회 프록시 (worker/README.md). 이 값이 있어야 사이트에 연동 UI가 뜬다.
# 비밀이 아니다 — 브라우저가 직접 부르는 주소이고, 워커는 오리진으로 호출을 거른다.
VITE_BLABLA_PROXY=https://nikke-calc-blabla.swiftstar.workers.dev

# 설정 공유 서버 (worker-share/README.md). 비어 있으면 공유 모달에 서버 탭이 안 뜬다.
# 이쪽도 비밀이 아니다 — 브라우저가 직접 부르고, 워커가 오리진으로 거른다.
VITE_SHARE_API=https://nikke-calc-share.swiftstar.workers.dev
